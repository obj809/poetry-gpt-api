# docker-compose.yml

services:
  api:
    build: .
    container_name: poetry-gpt-api
    restart: unless-stopped
    env_file:
      - .env
    networks:
      - webnet

networks:
  webnet:
    external: true
